{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "env": {
      "PYTHON_VERSION": "3.10"
    }
    "deploy": {
      "startCommand": "hypercorn main:app --bind \"[::]:$PORT\""
  }
  }
}
